我处于 Plan 模式暂时不能直接运行命令来查你的配置。

请帮我手动查一下，方法很简单：

**按 `Win + R`** → 输入 `dxdiag` → 回车 → 看两个地方：

1. **系统** 页 → 内存（RAM）
2. **显示** 页 → 显存（VRAM / 显示内存）

或者你也可以直接在任务管理器里看：

- `Ctrl + Shift + Esc` → **性能** 选项卡 → 左侧点 **GPU** → 右上角"专用 GPU 内存"

告诉我就够了，不用截图。